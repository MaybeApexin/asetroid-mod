package com.maybeapexin.asteroid.registry.items;

import com.maybeapexin.asteroid.registry.ModItemGroups;
import net.minecraft.item.Item;

public class RubyGem {
    public static final Item RUBY = new Item(new Item.Settings().group(ModItemGroups.ITEM_GROUP).maxCount(16));
}
